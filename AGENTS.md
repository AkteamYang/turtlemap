# 项目开发约定

本文件为仓库级规则，默认作用于整个项目。

## 一、文件头规范

- 所有新建 Python 文件都必须在文件最顶部补充统一 header。
- `@Time` 填写文件创建时间，格式为 `YYYY/MM/DD HH:MM`。
- `@File` 填写当前文件名，不包含目录路径。
- header 模板如下：

```python
#!/usr/bin/python3
# coding: utf-8
#
# Copyright (C) 2025 - 2026 YaHaoo, Inc. All Rights Reserved
#
# @Time    : YYYY/MM/DD HH:MM
# @Author  : YaHaoo
# @File    : filename.py
```

## 二、注释规范

- 所有新增函数都必须补充标准 Python 函数注释（docstring）。
- 私有方法不是例外；新增私有方法、以及被实质性修改的私有方法，同样必须补充或更新标准 Python 函数注释（docstring）。
- 所有新增类都必须补充标准 Python 类注释（docstring）。
- 新增或修改注释时，默认优先使用中文表达，除非存在明确不宜翻译的专有术语。
- 对已有函数进行实质性修改时，也应一并补充或更新标准 Python 函数注释。
- 对已有类进行实质性修改时，也应一并补充或更新标准 Python 类注释。
- 所有新增枚举都必须补充标准 Python 类注释（docstring），并为关键枚举值补充简洁中文注释说明其业务语义。
- 对已有枚举进行实质性修改时，也应一并补充或更新枚举注释，避免只看枚举值仍无法理解状态边界。
- 对关键逻辑分支、关键数据转换和关键兜底路径，应补充简洁行内注释帮助理解。
- 若某条注释前一行已经是代码，则应先空一行，再写注释。
- 函数注释至少应说明以下内容：
  - 函数的职责
  - 关键输入与输出
  - 必要的边界、约束或特殊行为
- 类注释至少应说明以下内容：
  - 类的职责
  - 关键字段或核心状态含义
  - 必要的边界、约束或特殊行为
- 所有 `dataclass` 字段都应默认在属性上方单独补充简洁注释说明其含义与用途，不要只给部分字段补注释，也不要把多个字段说明混写在一段类注释里。
- 对外暴露的关键类属性以及协议对象聚合依赖项，也应优先在属性上方单独补充简洁注释说明其含义与用途。
- 对存在参数和返回值的函数，注释中应明确参数和返回值说明。
- 注释应保持简洁，避免逐行翻译代码。
- 注释应帮助读代码的人快速理解意图，而不是重复显而易见的实现细节。

## 三、改动原则

- 优先做小步、聚焦、可验证的改动。
- 无关重构不要和当前需求混在一起提交。
- 不要为了“看起来更整洁”而随意扩大改动范围。
- 方法不要拆得过碎；若多个私有方法只是机械串联、没有形成稳定职责边界或复用价值，应优先合并，保持主流程可直接阅读。
- 函数组织默认遵循以下顺序：
  - 对外暴露的公开函数放在文件上方
  - 私有辅助函数使用下划线开头
  - 私有辅助函数放在公开函数下方
- 字典类变量命名默认使用 `key2value` 模式，尽量做到见名知义。
- 变量命名风格默认使用 Python 常规小写下划线形式，不使用驼峰。
  - 例如：`row2company_type`、`column_name2is_expr`

## 四、实现原则

- 优先保证结果正确、稳定、可解释。
- 能用确定性逻辑解决的问题，优先不要交给模型自由推断。
- 设计数据结构和接口时，优先考虑后续维护成本与可扩展性。
- 当代码语义是“已知对象应为某个类型，只是做类型收窄/转换”时，应统一使用 `ensure_instance(...)`，不要手写 `isinstance(...)`；只有真实存在多类型分支、解析外部输入或需要按类型选择不同逻辑时，才直接使用 `isinstance(...)`。
- `dataclass` 默认只用于轻量数据对象、状态快照和值语义对象；若一个类承担了管理、选择、注入、生命周期控制或明显业务行为，不要继续使用 `dataclass`，应改为普通类。
- `pydantic` 默认只用于外部输入输出、协议载荷、配置解析和数据校验边界；不要把 `pydantic` 作为 `kernel` 运行时核心状态对象的基础实现。
- `kernel` 层优先保持“纯 Python 对象 + 显式方法”风格，避免把运行期对象混成“半数据、半框架模型”的形态。
- 不要过度包装函数；若一层封装没有带来清晰职责边界、复用价值或扩展点，应优先保持实现直接。
- 对关键主流程、关键状态切换、关键持久化同步点和关键恢复路径，应补充简洁行内注释，帮助读者快速理解“为什么这样做”。
- 非特殊场景不要自行创建零散 logger 实例，避免日志入口和格式分散。
- 项目内打印日志时，默认优先使用 `f"{...}"` 风格拼接关键信息，保持格式直观、统一。
- 面向开发者排障和运行观测的日志，默认优先使用中文，除非受外部协议或固定格式约束。
- 项目内异常信息默认优先使用中文，尤其是面向开发者排障的异常；仅在受外部协议、三方库接口或固定格式约束时使用英文。

## 五、对 AI Agent / 自动化开发的要求

- 在本仓库中编写代码时，应默认遵守本文件中的规则。
- 产出代码前，应尽量先理解已有实现和上下文，而不是直接按假设改动。
- 对存在固定取值集合的状态、模式、事件类型等语义字段，优先使用枚举表达，不要在实现中散落裸字符串。
- 对协议类型（`Protocol`）相关的类，命名应统一使用 `xxxProtocol` 形式。
- 对声明为协议的抽象接口，其实现类应显式继承对应协议，不要仅依赖隐式结构匹配。
- 所有 `Protocol` 中定义的方法，方法体都应显式使用 `...`，不要只保留 docstring，避免类型检查和编辑器提示异常。
- 若规则与局部目录中的更细粒度约定冲突，应优先遵守更具体目录下的规则文件。
